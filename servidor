# Configurações do Servidor
PORT=3001
NODE_ENV=development

# JWT Secret (use uma chave forte em produção)
JWT_SECRET=sua_chave_jwt_super_secreta_aqui_2024

# MongoDB
MONGODB_URI=mongodb://localhost:27017/royal_slots
# Para MongoDB Atlas (produção):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/royal_slots

# Configurações de RTP
DEFAULT_RTP=0.88
MIN_RTP=0.85
MAX_RTP=0.92

# Configurações de Segurança
BCRYPT_ROUNDS=12
JWT_EXPIRATION=7d

# Configurações de Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Configurações de Pagamento (para integração futura)
PAYMENT_GATEWAY_API_KEY=your_payment_gateway_key
PAYMENT_GATEWAY_SECRET=your_payment_gateway_secret

# Configurações de Email (para notificações)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# Logs
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Configurações de Cache Redis (opcional)
REDIS_URL=redis://localhost:6379

# Configurações de Monitoramento
SENTRY_DSN=your_sentry_dsn_here

# Configurações específicas do jogo
HOUSE_EDGE=0.12
MAX_BET_AMOUNT=1000
MIN_BET_AMOUNT=1
MAX_DAILY_WITHDRAWAL=10000
